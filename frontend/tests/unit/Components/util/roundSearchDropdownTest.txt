import { mount } from '@vue/test-utils'
import roundSearchDropdown from "@/components/util/RoundSearchDropdown.vue";

// kan file niet inladen in testomgeving, dus testen werken niet

describe('roundSearchDropdown.vue', () => {

  let wrapper;

  beforeEach(() => {
    wrapper = mount(roundSearchDropdown);
  })

  it('render', () => {
    expect(wrapper.exists()).toBe(true);
  })

})
